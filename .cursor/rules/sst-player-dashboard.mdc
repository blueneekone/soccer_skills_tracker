---
description: Player OS dashboard conventions
globs: src/**/player/dashboard/**,src/routes/(app)/player/dashboard/**
alwaysApply: false
---

# Player OS dashboard

## Layout (OperativeHub)

`HUDContainer` wraps the dashboard shell. Inside `OperativeHub`:

| Zone | Grid span | Component |
|------|-----------|-----------|
| Command main | `bento-span-12 md:bento-span-8` | `IdentityBentoModule` + `HudMetricsPanel` (vectors) |
| Mission rail | `bento-span-12 md:bento-span-4` | `ActiveBounties` embedded |

Below the hub row: single **analytics deck** (`player-analytics-deck`) = `VanguardProtocolPanel` (radar always) + compact capsules strip — see [`docs/vision/PLAYER_OS.md`](../../docs/vision/PLAYER_OS.md).

**Route shell:** `src/routes/(app)/player/dashboard/+page.svelte`

## Components (do not rename)

- `IdentityBentoModule` — operative identity, avatar, rank
- `HudMetricsPanel` — PAC–AGI vector strip (embedded); streak/XP live in `IdentityBentoModule`
- `ActiveBounties` — mission list (use `deduplicateById` for dedup)
- `VanguardProtocolPanel` — protocol / compliance capsule
- `HUDContainer` — shared HUD chrome from `src/lib/components/hud/HUDContainer.svelte`

## CSS

**Sprint 2.8 — Player Dossier:** `src/lib/styles/player-dossier.css` (canonical `--pd-*` tokens under `.player-dossier-root`) + `src/lib/styles/player-dashboard-hud.css`.

Dual accent: gold action (`--pd-accent-action`), teal data (`--pd-accent-data`). Black canvas `--pd-bg`; lifted panels `--pd-panel`. Chamfer on hero CTA + avatar ring only.

**Z-depth constitution** per [`docs/vision/PLAYER_OS_MATERIAL_SPATIAL.md`](../../docs/vision/PLAYER_OS_MATERIAL_SPATIAL.md):

- Canvas/atmosphere scanlines OK; **no scanlines on mission text**
- Glass/inset wells allowed in identity stage, radar well, inputs — not competing glass stacks on command shell

**Premium track 2.16–2.19** (cinematic orchestration) — current planned sprint: **2.16** — see `ROADMAP.md`.

## Tests to extend

When changing this area, extend (never delete):

- `playerHudSprint*.test.ts`
- `playerDashboard.layout.test.ts`
- `playerDashboard.hud.test.ts`
