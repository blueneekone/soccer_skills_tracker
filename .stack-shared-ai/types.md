# Types

### `.svelte-kit/non-ambient.d.ts`

- (interface) AppTypes — 100 field(s)
- (interface) HTMLAttributes<T> — 15 field(s)

### `.svelte-kit/types/src/routes/$types.d.ts`

- (type) LayoutData — Expand<Omit<LayoutParentData, keyof LayoutParentData & Ensur
- (type) LayoutLoad<OutputData extends OutputDataShape<LayoutParentData> — OutputDataShape<LayoutParentData>> = Kit.Load<LayoutParams, 
- (type) LayoutLoadEvent — Parameters<LayoutLoad>[0]
- (type) LayoutProps — { params: LayoutParams
- (type) LayoutServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/$types.d.ts`

- (type) LayoutData — Expand<Omit<LayoutParentData, keyof LayoutParentData & Ensur
- (type) LayoutLoad<OutputData extends OutputDataShape<LayoutParentData> — OutputDataShape<LayoutParentData>> = Kit.Load<LayoutParams, 
- (type) LayoutLoadEvent — Parameters<LayoutLoad>[0]
- (type) LayoutProps — { params: LayoutParams
- (type) LayoutServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/admin/$types.d.ts`

- (type) LayoutData — Expand<LayoutParentData>
- (type) LayoutProps — { params: LayoutParams
- (type) LayoutServerData — null
- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/admin/audit-log/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/admin/organizations/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/admin/organizations/[clubId]/$types.d.ts`

- (type) EntryGenerator — () => Promise<Array<RouteParams>> | Array<RouteParams>
- (type) LayoutData — Expand<LayoutParentData>
- (type) LayoutProps — { params: LayoutParams
- (type) LayoutServerData — null
- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/admin/organizations/[clubId]/teams/$types.d.ts`

- (type) EntryGenerator — () => Promise<Array<RouteParams>> | Array<RouteParams>
- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/admin/organizations/[clubId]/teams/[teamId]/roster/$types.d.ts`

- (type) EntryGenerator — () => Promise<Array<RouteParams>> | Array<RouteParams>
- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/admin/overview/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/admin/recruiters/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/admin/system-settings/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/admin/users/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/challenges/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/coach/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/coach/drills/$types.d.ts`

- (type) PageData — Expand<Omit<PageParentData, keyof PageParentData & EnsureDef
- (type) PageLoad<OutputData extends OutputDataShape<PageParentData> — OutputDataShape<PageParentData>> = Kit.Load<RouteParams, Pag
- (type) PageLoadEvent — Parameters<PageLoad>[0]
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/coach/forge/$types.d.ts`

- (type) PageData — Expand<Omit<PageParentData, keyof PageParentData & EnsureDef
- (type) PageLoad<OutputData extends OutputDataShape<PageParentData> — OutputDataShape<PageParentData>> = Kit.Load<RouteParams, Pag
- (type) PageLoadEvent — Parameters<PageLoad>[0]
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/coach/match-day/$types.d.ts`

- (type) PageData — Expand<Omit<PageParentData, keyof PageParentData & EnsureDef
- (type) PageLoad<OutputData extends OutputDataShape<PageParentData> — OutputDataShape<PageParentData>> = Kit.Load<RouteParams, Pag
- (type) PageLoadEvent — Parameters<PageLoad>[0]
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/coach/scouting/$types.d.ts`

- (type) PageData — Expand<Omit<PageParentData, keyof PageParentData & EnsureDef
- (type) PageLoad<OutputData extends OutputDataShape<PageParentData> — OutputDataShape<PageParentData>> = Kit.Load<RouteParams, Pag
- (type) PageLoadEvent — Parameters<PageLoad>[0]
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/coach/tactical/$types.d.ts`

- (type) PageData — Expand<Omit<PageParentData, keyof PageParentData & EnsureDef
- (type) PageLoad<OutputData extends OutputDataShape<PageParentData> — OutputDataShape<PageParentData>> = Kit.Load<RouteParams, Pag
- (type) PageLoadEvent — Parameters<PageLoad>[0]
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/coach/trial-builder/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/compliance/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/director/$types.d.ts`

- (type) LayoutData — Expand<LayoutParentData>
- (type) LayoutProps — { params: LayoutParams
- (type) LayoutServerData — null
- (type) PageData — Expand<Omit<PageParentData, keyof PageParentData & EnsureDef
- (type) PageLoad<OutputData extends OutputDataShape<PageParentData> — OutputDataShape<PageParentData>> = Kit.Load<RouteParams, Pag
- (type) PageLoadEvent — Parameters<PageLoad>[0]
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/director/compliance/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/home/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/messages/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/operative/profile/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/parent/household/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/parent/log-workout/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/parent/vpc/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/passport/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/player/armory/$types.d.ts`

- (type) PageData — Expand<Omit<PageParentData, keyof PageParentData & EnsureDef
- (type) PageLoad<OutputData extends OutputDataShape<PageParentData> — OutputDataShape<PageParentData>> = Kit.Load<RouteParams, Pag
- (type) PageLoadEvent — Parameters<PageLoad>[0]
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/player/dashboard/$types.d.ts`

- (type) PageData — Expand<Omit<PageParentData, keyof PageParentData & EnsureDef
- (type) PageLoad<OutputData extends OutputDataShape<PageParentData> — OutputDataShape<PageParentData>> = Kit.Load<RouteParams, Pag
- (type) PageLoadEvent — Parameters<PageLoad>[0]
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/player/tracker/$types.d.ts`

- (type) PageData — Expand<Omit<PageParentData, keyof PageParentData & EnsureDef
- (type) PageLoad<OutputData extends OutputDataShape<PageParentData> — OutputDataShape<PageParentData>> = Kit.Load<RouteParams, Pag
- (type) PageLoadEvent — Parameters<PageLoad>[0]
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/player/workout/$types.d.ts`

- (type) PageData — Expand<Omit<PageParentData, keyof PageParentData & EnsureDef
- (type) PageLoad<OutputData extends OutputDataShape<PageParentData> — OutputDataShape<PageParentData>> = Kit.Load<RouteParams, Pag
- (type) PageLoadEvent — Parameters<PageLoad>[0]
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/recruiter/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/settings/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/stats/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/tracker/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/tutor/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/upgrade/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(app)/vpc-pending/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(auth)/reset/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(marketing)/$types.d.ts`

- (type) LayoutData — Expand<Omit<LayoutParentData, keyof LayoutParentData & Ensur
- (type) LayoutLoad<OutputData extends OutputDataShape<LayoutParentData> — OutputDataShape<LayoutParentData>> = Kit.Load<LayoutParams, 
- (type) LayoutLoadEvent — Parameters<LayoutLoad>[0]
- (type) LayoutProps — { params: LayoutParams
- (type) LayoutServerData — null
- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(marketing)/features/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/(marketing)/pricing/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/api/dd2828/$types.d.ts`

- (type) RequestEvent — Kit.RequestEvent<RouteParams, RouteId>
- (type) RequestHandler — Kit.RequestHandler<RouteParams, RouteId>

### `.svelte-kit/types/src/routes/api/ingest/$types.d.ts`

- (type) RequestEvent — Kit.RequestEvent<RouteParams, RouteId>
- (type) RequestHandler — Kit.RequestHandler<RouteParams, RouteId>

### `.svelte-kit/types/src/routes/club/[slug]/$types.d.ts`

- (type) EntryGenerator — () => Promise<Array<RouteParams>> | Array<RouteParams>
- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/complete-profile/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/consent/[token]/$types.d.ts`

- (type) EntryGenerator — () => Promise<Array<RouteParams>> | Array<RouteParams>
- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/login/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/privacy/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/recruit/$types.d.ts`

- (type) LayoutData — Expand<LayoutParentData>
- (type) LayoutProps — { params: LayoutParams
- (type) LayoutServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/recruit/[playerKey]/$types.d.ts`

- (type) EntryGenerator — () => Promise<Array<RouteParams>> | Array<RouteParams>
- (type) PageData — Expand<Omit<PageParentData, keyof Kit.LoadProperties<Awaited
- (type) PageLoad<OutputData extends OutputDataShape<PageParentData> — OutputDataShape<PageParentData>> = Kit.Load<RouteParams, Pag
- (type) PageLoadEvent — Parameters<PageLoad>[0]
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/setup/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `.svelte-kit/types/src/routes/terms/$types.d.ts`

- (type) PageData — Expand<PageParentData>
- (type) PageProps — { params: RouteParams
- (type) PageServerData — null
- (type) RequiredKeys<T> — { [K in keyof T]-?: {} extends { [P in K]: T[K] } ? never : 
- (type) Snapshot<T = any> — Kit.Snapshot<T>

### `src/lib/actions/swipe.ts`

- (interface) SwipeOptions — 7 field(s)

### `src/lib/components/coach/TacticalEngine.svelte.ts`

- (type) TacticalGridHost — {
- (type) TacticalWarRoomModel — ReturnType<typeof createTacticalWarRoom>

### `src/lib/services/commerce.svelte.ts`

- (type) PaymentStatus — 'none' | 'pending' | 'processing' | 'paid' | 'failed'
- (interface) RegistrationRecord — 5 field(s)

### `src/lib/services/comms.svelte.ts`

- (interface) BroadcastMessageInput — 3 field(s)
- (interface) BroadcastResult — 6 field(s)
- (interface) BroadcastTarget — 4 field(s)
- (type) BroadcastTargetType — 'team' | 'channel'
- (type) SendPhase — 'idle' | 'sending' | 'success' | 'error'

### `src/lib/services/integrations.svelte.ts`

- (interface) Episode — 6 field(s)
- (interface) NewsArticle — 7 field(s)
- (type) NewsCategory — 'general' | 'us' | 'youth'
- (interface) NowPlaying — 3 field(s)
- (interface) Podcast — 8 field(s)

### `src/lib/services/inviteService.ts`

- (type) ConsumeInviteResult — {
- (type) GenerateInviteResult — {

### `src/lib/services/league.svelte.ts`

- (type) CompleteMatchInput — Omit<
- (type) CreateFixtureInput — Omit<

### `src/lib/services/org.svelte.ts`

- (interface) AssignCoachInput — 2 field(s)
- (interface) CreateTeamInput — 3 field(s)
- (interface) OrgMetrics — 4 field(s)

### `src/lib/services/scholar.svelte.ts`

- (interface) AcademicRecord — 9 field(s)
- (type) EligibilityStatus — 'eligible' | 'probation' | 'ineligible' | 'unknown'
- (interface) GpaTrendPoint — 3 field(s)
- (interface) Subject — 5 field(s)

### `src/lib/services/weather.svelte.ts`

- (type) AlertLevel — 'NORMAL' | 'CAUTION' | 'DANGER'
- (type) DeploymentStatus — 'GO' | 'HOLD' | 'NO-GO'
- (interface) LightningStatus — 6 field(s)
- (interface) WeatherCurrent — 9 field(s)
- (interface) WeatherSnapshot — 6 field(s)

### `src/lib/states/ArmoryEngine.svelte.ts`

- (type) ArmoryEngineInit — {
- (type) ArmoryTier — {
- (type) ScoutsSix — {
- (type) XpHistoryEntry — {

### `src/lib/states/war-room/TacticalInputEngine.svelte.ts`

- (type) AnchorDrag — {
- (type) CapturePair — { el: Element | null
- (type) RibbonBox — { value: string }
- (type) RouteBodyDrag — { routeId: string
- (interface) TacticalPointerHost — 53 field(s)
- (type) TrailPt — { x: number

### `src/lib/states/war-room/tacticalGridPlayback.svelte.ts`

- (interface) TacticalPlaybackHost — 12 field(s)
- (type) TrailPt — { x: number

### `src/lib/states/war-room/tacticalGridRadial.svelte.ts`

- (type) RadialSlotSource — 'xi' | 'bench' | 'opp'
- (interface) TacticalRadialDeps — 10 field(s)

### `src/lib/states/war-room/types.ts`

- (interface) TacticalCartridge — 10 field(s)
- (type) TacticalRoute — {
- (type) TacticalToken — {

### `src/lib/types/coppa.ts`

- (type) ConsentAction — 'granted' | 'denied'
- (type) ConsentLogAction — 'email_sent' | 'granted' | 'denied' | 'expired' | 'resend'
- (interface) ConsentLogDoc — 11 field(s)
- (interface) ConsentTokenDoc — 8 field(s)
- (type) CoppaStatus — 'pending' | 'granted' | 'denied'
- (interface) CoppaUserFields — 5 field(s)
- (interface) SendConsentEmailInput — 1 field(s)
- (interface) VerifyConsentInput — 2 field(s)
- (interface) VerifyConsentResult — 3 field(s)

### `src/lib/types/league.ts`

- (type) AnyDate — Date | FirestoreTimestamp | string | number
- (interface) EnrichedFixture — 2 field(s)
- (interface) Fixture — 24 field(s)
- (type) FixtureStatus — 'Scheduled' | 'Completed' | 'Cancelled' | 'Postponed'
- (type) FixtureType — 'League' | 'Tournament' | 'Friendly'
- (interface) MatchResult — 17 field(s)
- (interface) Opponent — 15 field(s)
- (interface) OpponentStats — 7 field(s)
- (interface) PlayerMatchStats — 7 field(s)
- (interface) Season — 15 field(s)
- (interface) ThreatAssessment — 4 field(s)
- (type) ThreatLevel — 'LOW' | 'MEDIUM' | 'HIGH' | 'UNKNOWN'

### `src/lib/types/tenant.ts`

- (interface) ActiveMissionDoc — 13 field(s)
- (interface) FirestoreTimestamp — 3 field(s)
- (interface) FixtureDoc — 18 field(s)
- (interface) InviteDoc — 18 field(s)
- (interface) OpponentDoc — 15 field(s)
- (interface) OrganizationDoc — 8 field(s)
- (interface) SeasonDoc — 9 field(s)
- (type) TenantClaims — VanguardClaims
- (type) TenantRole — | 'global_admin'    // platform operator — cross-tenant
- (interface) TenantTeam — 9 field(s)
- (interface) TenantUser — 13 field(s)
- (interface) VanguardClaims — 6 field(s)
- (interface) XpHistoryEntry — 4 field(s)

### `src/lib/utils/time.ts`

- (type) AnyDate — | Date
- (interface) FixtureDateDisplay — 6 field(s)
