echo "The instruction is:"
echo "Ensure you write camelCase subscriptionStatus (e.g., 'active', 'past_due') strictly to the organizations and license_entitlements collections (snake_case database updates are banned)."
