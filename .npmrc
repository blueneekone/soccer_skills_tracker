; @firebase/rules-unit-testing@4 declares a peer of firebase@^11, but the app is on
; firebase@^12. The emulator API we use is unaffected, so relax strict peer resolution
; for `npm ci`/`npm install` (root + per-codebase installs) instead of failing CI.
legacy-peer-deps=true
