### SSTRACKER (Nexus Command) - Master Project Roadmap
**Architecture Philosophy:** Zero-Liability PII, Exception-Only Administration, Dynamic Multi-Sport Scaling, Uncompromised RPG Gamification, and Deep Emotional Intelligence (EQ) Protections.

#### PHASE 0: Global Aesthetic & Layout Synchronization
*Objective: Lock in the multi-billion-dollar SIEM aesthetic and enterprise responsiveness before touching data layers.*
* [x] **Sprint 0.1: The Bento Grid Lock.** Transition all core layouts to the 12-column asymmetric Bento Grid using fluid `clamp()` spacing to prevent mobile squishing. Forcefully apply the `.dark-form-surface` utility to permanently resolve dark-mode contrast barriers [4].
* [x] **Sprint 0.2: Component Standardization.** Execute a global sweep to replace fragmented button and tab classes. Purge the legacy 'pills' UI on the Global Users page and standardize all sub-navigation using the horizontal `.tab-nav` component [4].

#### PHASE 1: Data Plane & Core Architecture Stabilization
*Objective: Eradicate legacy technical debt, enforce Svelte 5 strictness, and lock down the multi-tenant database routing.*
* [] **Sprint 1.1: Svelte 5 Vanguard Trinity Enforcement.** Audit all viewports to ensure they strictly fracture into The Shell (`+page.svelte`), The Brain (`*Engine.svelte.ts`), The Glass (`*Arena.svelte`), and The HUD (`*HUD.svelte`). Enforce the 80-line function maximum [4].
* [] **Sprint 1.2: PWA Caching & Vite Pipeline.** Modify `vite.config.js` to utilize cryptographic file-hash versioning (`[name].[hash].js`) to solve the cache poisoning issue. Rewrite the Service Worker config to bypass all requests explicitly destined for Firebase Identity endpoints or `/auth/` routes [10].
* [] **Sprint 1.3: Cloud Functions Architecture.** Ensure all 7 serverless codebases (core, rl, compliance, platform, commerce, integrations, monolith) are correctly split to minimize deployment blast radius. Remove `minInstances` configurations to return the backend to a true scale-to-zero economic model [4].
* [] **Sprint 1.4: Cell-Based Firestore Isolation.** Enforce API gateway routing to isolate large National Governing Bodies into distinct Firestore database cells based on `cellId` JWT claims, eliminating noisy-neighbor throttling [4].

#### PHASE 2: Director OS & Operations Command
*Objective: Perfect the B2B administrative shell, financial routing, and logistical conflict engines.*
* [] **Sprint 2.1: Stripe Connect & Seat Licensing.** Implement the Stripe Webhook Bouncer to listen strictly for `checkout.session.completed`, `customer.subscription.updated`, and `customer.subscription.deleted`. Audit the `license_entitlements/{clubId}` schema to mathematically guarantee `active_seats` cannot exceed `seats_limit` via atomic server-side transactions [4].
* [] **Sprint 2.2: Field Ops Conflict Engine.** Finalize the `secureBookField` backend transaction to ensure that double-booking a facility triggers an immediate overlap rejection before writing to the database [4].
* [] **Sprint 2.3: Checkr Staff Vetting.** Wire the Checkr Client-Hosted SDK into the onboarding flow, ensuring that Coach clearance is strictly evaluated via JWT claims before unlocking the Coach OS [4].
* [] **Sprint 2.4: 12-Column Asymmetric Grid.** Force the Director OS layout into an 8-column Primary Canvas and a 4-column Sidecar, utilizing minmax clamp math to ensure horizontal data table swiping on mobile devices without squishing [4].
* [] **Sprint 2.5: Absolute Data Zeroing.** Open `src/routes/(app)/admin/overview/+page.svelte`. Permanently delete all mock constants and purge the `hasSignal` fallback logic. Force all widgets to read directly from `analytics/platform_totals` and live collections, gracefully rendering 0 if data is missing [4].
* [] **Sprint 2.6: API Activation & Webhook Telemetry.** In `src/lib/stores/featureFlags.svelte.js`, set `stripeCheckout` and `checkrVetting` feature flags to true. Create a script to inject realistic payloads into the `webhook_events` collection to populate the System Settings integration table [4].

#### PHASE 3: Parent OS & Legal Compliance Moat (COPPA)
*Objective: Seal the platform's liability loop through verifiable consent and automated data shredding.*
* [] **Sprint 3.1: Verifiable Parental Consent (VPC).** Finalize the WebAuthn (FaceID/TouchID) biometric enclave attestation. Ensure the `deriveIsConsented()` flag gates all data-collection routes, forcefully redirecting unverified minors to a `/vpc-pending` holding state [5].
* [] **Sprint 3.2: SafeSport "Shadow CC" Comms Hub.** Audit the `sendChannelMessage` callable. Ensure it explicitly blocks direct 1-on-1 adult-to-minor messages by automatically resolving and injecting the linked parent's email into the `ccParentEmails` array for all squad threads [5].
* [] **Sprint 3.3: PII Burn Protocol (TTL Shredder).** Finalize the `shredSensitiveData` daily cron job. It must autonomously overwrite PII inside the `users` and `passports` collections after 24 hours of inactivity while strictly exempting the `consents` collection for legal multi-year retention [5].

#### PHASE 4: Coach OS & The Tactical Engine
*Objective: Transition coaches from static clipboards to dynamic, digital intent deployment.*
* [] **Sprint 4.1: Spatial Drill Designer.** Fortify the HTML5 Fabric.js canvas to utilize SVG bounding-box scaling and `preserveAspectRatio="xMidYMid slice"`, preventing drawn tactical routes and player tokens from warping on ultra-wide monitors or mobile devices [5].
* [] **Sprint 4.2: The Intent Engine.** Ensure the drag-and-drop curriculum builder successfully packages drill assignments as "Homework" and maps them directly to the active `team_assignments` inbox of the targeted roster [5].

#### PHASE 5: Player OS & Recruiter Marketplace
*Objective: Finalize the gamification loop, video verification, and the B2B2C scouting pipeline.*
* [] **Sprint 5.1: The "Void Contract" UI.** Audit the Player OS to ensure it adheres to a minimum 40% "Void Density" (slate/black backgrounds) to maintain the Tactical SIEM aesthetic. Replace all procedural avatar circles with deterministic, UID-seeded HUD rings utilizing 24px border radii [5].
* [] **Sprint 5.2: The Dopamine Engine.** Decouple telemetry tracking from browser memory by enforcing Firestore `writeBatch` commits. Connect the asynchronous HTML5 canvas-confetti burst to trigger only upon a successfully verified backend data save [5].
* [] **Sprint 5.3: Snowflake Skill Tree & RPG Metrics.** Verify the backend mapping of "Drill-as-Node" primitives to the synthetic "Scout's Six" attributes (Pace, Power, Agility, etc.). Ensure the radar chart dynamically updates consecutive-day activity streaks [5].
* [] **Sprint 5.4: Video Trials & Sanitized Recruiter Index.** Lock down Firebase Storage rules to enforce a 50MB file limit on 30-second video trial uploads. Ensure the `syncPublicPlayerProfile` trigger securely builds read-only prospect cards so subscribed recruiters can filter the global index without exposing minors' direct PII [5].

#### PHASE 6: Scale & Multi-Sport Expansion
*Objective: Move beyond youth soccer and transform the platform into a universal athletic operating system capable of managing any sport.*
* [] **Sprint 6.1: Dynamic Sport Templates.** Restructure the underlying database schemas to support sport-specific drill libraries, allowing the Svelte 5 engine to instantly scale the platform to support Basketball, Football, Lacrosse, Volleyball, and beyond without requiring separate codebases [6, 7].
* [] **Sprint 6.2: Dynamic XP Balancing.** Engineer adaptive algorithms to balance the Octalysis Gamification math, ensuring that Grit XP and progression rings remain mathematically fair and psychologically rewarding, regardless of the sport being tracked (e.g., balancing a basketball repetition vs a soccer minute) [6, 7].
* [] **Sprint 6.3: Custom Branding Engine.** Open up the `--brand-primary` and `--brand-accent` CSS variables to the Director OS. Allow massive clubs and recreation centers to upload their own hex codes and logos, completely overriding the platform's default UI colors to secure absolute white-label B2B dominance [6, 7].