# Agent 17 — native shell

**Branch:** overnight/native-shell

**Owns:** Capacitor config, `ios/`, `android/`, `docs/NATIVE_SHELL.md`

## Task

Capacitor 6 wrap sstracker.app; parent-first default route; build docs for acquirer; NO App Store submission tonight. `nativeShellLaunch.test.ts`.

---

Universal rules: Append SLICE_LOG.md only (agent 21-orch may sync ROADMAP). Do NOT build drag-and-drop club website CMS. Each commit: npm test (slice), npm run check (log count), npm run build. Do not ask questions.
