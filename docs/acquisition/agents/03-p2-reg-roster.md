# Agent 03 — P2 registration roster

**Branch:** overnight/p2-reg-roster

**Owns:** registration→roster assign flow (not TeamsTab export panel)

## Task

Registration→roster assign UX — assign paid registrants to team roster/player_lookup. Tests: registrationLaunch.test.ts or new file.

---

Universal rules: Append SLICE_LOG.md only (agent 21-orch may sync ROADMAP). Do NOT build drag-and-drop club website CMS. Each commit: npm test (slice), npm run check (log count), npm run build. Do not ask questions.
