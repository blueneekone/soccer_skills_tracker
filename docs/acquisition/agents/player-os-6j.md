# Agent — player-os-6j

**Branch:** `closure/player-os-6j`

**Owns:** `src/lib/styles/player-dossier.css`, `src/routes/(app)/player/**/+page.svelte`, `src/lib/components/player/dashboard/__tests__/playerHudSprint234.test.ts`

## Task

Register **J-02**, **J-06**, **J-07**, **J-10** — remaining Z2 depth / void pass:

1. Apply 6j-a/b parity to any route still reading flat matte (`pd-page-panel` without deck raise).
2. Reduce `:global(.bento-card/.card/.ec-panel)` fight on PlayerShell canvas where scoped fix is ≤5 files.
3. Stats route: align achievement matrix / seg toggles toward investigation workspace (6l targets) without Chart.js migration.
4. Extend `playerHudSprint234.test.ts` or add route guard for deck tokens.

**Out of scope:** Owner VA screenshot sign-off (J-04).

---

Universal rules: Append SLICE_LOG.md only. Do NOT build rejects R-01–R-03. Each commit: `npm test -- src/lib/components/player/dashboard/__tests__/playerHudSprint234.test.ts`, npm run check, npm run build. Do not ask questions.
