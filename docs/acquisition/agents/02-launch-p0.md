# Agent 02 — launch P0 fixes

**Branch:** overnight/launch-p0

**Owns:** `src/routes/(app)/parent/household/+page.svelte`, `src/routes/(app)/vpc-pending/+page.svelte`, `scripts/dev-tenant-reset.mjs`

## Task

Fix `&amp;` button text, parent copy, vpc-pending legacy director copy, tenant reset householdId + inviteCode.

---

Universal rules: Append SLICE_LOG.md only (agent 21-orch may sync ROADMAP). Do NOT build drag-and-drop club website CMS. Each commit: npm test (slice), npm run check (log count), npm run build. Do not ask questions.
