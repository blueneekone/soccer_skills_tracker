# Agent 15 — live stream embed

**Branch:** overnight/live-stream

**Owns:** schedule/match-day stream fields + embed UI

## Task

`liveStreamUrl` on events/match_sessions; coach paste YouTube/Vimeo/Mux; parent Watch live; iframe allowlist + teen guard. `liveStreamLaunch.test.ts`.

---

Universal rules: Append SLICE_LOG.md only (agent 21-orch may sync ROADMAP). Do NOT build drag-and-drop club website CMS. Each commit: npm test (slice), npm run check (log count), npm run build. Do not ask questions.
