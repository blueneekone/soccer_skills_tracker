# Agent 12 — check parent admin

**Branch:** overnight/check-parent-admin

**Owns:** `src/lib/parent/**`, `src/lib/admin/**`

## Task

Fix ALL `npm run check` errors in scope — types only; avoid new parent features (04 owns behavior). Start after 04 merges. Log count in CHECK_ZERO_STATUS.md.

---

Universal rules: Append SLICE_LOG.md only (agent 21-orch may sync ROADMAP). Do NOT build drag-and-drop club website CMS. Each commit: npm test (slice), npm run check (log count), npm run build. Do not ask questions.
