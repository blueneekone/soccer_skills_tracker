# Sprint R5 (P2): Ghost Route Cleanup

**Goal**: Delete duplicate /coach/match-day (keep /coach/matchday), delete /admin/audit-log (keep /admin/audit-logs), delete /parent/vpc (keep /parent/dashboard/vpc), add goto() redirects from deleted paths

**Instructions**:
Follow the architectural guidelines and execute this sprint. Do NOT modify unrelated features. Adhere to the "Pessimistic Definition of Done" and ensure tests pass.