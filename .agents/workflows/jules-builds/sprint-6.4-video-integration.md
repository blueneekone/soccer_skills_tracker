# Jules Sprint 6.4: Public Video Showcase Integration

## Objectives
Your task is to implement the Public Video Showcase Integration for Phase 6 of the SSTracker Architecture.
1. Wire `LandingHero.svelte` & `StakeholderBento.svelte` to support dynamic video modals.
2. Implement poster fallbacks and fast-start MP4 streaming.
3. Ensure the video playback is seamless and visually fits the high-end enterprise SaaS standard (no generic styling).

## Constraints & Requirements
- **Rule Adherence**: You MUST strictly adhere to the visual and aesthetic rules in `GEMINI.md` (Nuclear Americana Tech Noir, Void Black, Navy Slate).
- **Video Implementation**: You MUST use the `playwright-video-pipeline` output specifications for video links.
- **TDD Mandate**: You MUST ensure that any visual tests run 100% green and layout integrity is maintained.

## Definition of Done
When `npm run test` executes, it must return 100% green. Execute Svelte checks and typescript compilation and ensure 0 errors.

End of instructions.
