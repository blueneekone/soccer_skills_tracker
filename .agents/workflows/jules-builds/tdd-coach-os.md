---
description: TDD Swarm: Coach OS (Tactical SIEM)
---

### Workflow: Execute TDD Coach OS
**Priority:** P1 🔴 HIGH
**Persona Context:** Swarm Orchestrator (Architect, CSO, CDO, CRO)
**Mission:** Build the Coach OS, enforcing the HTML5 Spatial Drill Designer while mathematically gating all minor roster data behind the Checkr API Live Scan clearance.

#### Execution Steps

1. **Security & Data Gating (CSO):**
   * Hook into the `checkrCoachClearance.ts` API integration [cite: 1169, 1171].
   * Enforce strict backend rules: A coach MUST NOT be able to query `player_stats` or access the Squad Matrix unless their `clearanceStep` and AB 379 Concussion training certificates are verified via the Zero-Trust Document Vault.

2. **Tactical Architecture (Architect):**
   * Build the Tron War Room and Intent Engine [cite: 931].
   * Enforce the Vanguard Trinity Pattern (Shell, Brain, Glass, HUD). Fracture the dashboard so no single file or function exceeds 80 lines [cite: 1200, 1201].

3. **Aesthetics (CDO):**
   * Apply the Tactical SIEM aesthetic. Use strict 90-degree corners and Geist Mono typography for all layout panels [cite: 957].
   * Absolutely NO gamification chamfers or Action Gold CTAs are permitted on the coach's viewport [cite: 957].

4. **Validation (CRO):**
   * Execute Zero-Touch Authentication using the Firebase MCP server to bypass logins and run visual regression tests [cite: 1218].
   * Run `npm run check` to ensure zero Svelte 5 compiler errors [cite: 1203].